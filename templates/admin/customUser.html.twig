{% set url = ea_url()
    .setController('App\\Controller\\Admin\\UserCrudController')
    .setAction('detail')
    .setEntityId(field.value.id) %}

<div class="field-association">
	<a href="{{url}}">{{field.value.id ~ '-' ~ field.value.name ~ ' ' ~ field.value.firstname  }}</a>
</div>
